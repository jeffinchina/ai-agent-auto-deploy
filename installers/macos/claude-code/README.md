# Claude Code macOS Installer

Planned after the Windows Claude package is stable.

## Open Questions

- Preferred install manager: Homebrew, direct Node installer, or bundled portable runtime.
- Shell targets: zsh first, bash fallback.
- Provider configuration path and backup strategy for Claude Code on macOS.
- VM/test target: confirm a realistic macOS test environment before claiming support.

## Required Gates

- Static shellcheck-style validation.
- Fresh macOS VM install.
- DeepSeek minimal API check.
- Claude Code real smoke test.
