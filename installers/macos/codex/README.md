# Codex macOS Installer

Planned after Claude Code macOS installer design.

Before implementation, confirm the official Codex install path, runtime dependencies, provider configuration model, and supported macOS versions.
