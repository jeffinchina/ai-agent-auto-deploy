# Codex Windows Installer

Planned after Claude Code Windows v3.2.x is stable.

Before implementation, confirm the official Codex install path, runtime dependencies, provider configuration model, and supported Windows versions.
